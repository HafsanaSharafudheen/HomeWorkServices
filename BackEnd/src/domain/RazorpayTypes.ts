export interface BankDetails {
    name: string;
    accountNumber: string;
    ifsc: string;
  }
  
  export interface CreateContactParams {
    name: string;
    email: string;
    contact: string;
  }
  